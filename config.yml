input_dir: 'origin_data/test'  # 输入的根文件夹，里面所有的mp4都会被处理
output_dir: 'martial_data'  # 输出的根文件夹，会保留输入文件夹的结构
device: [0]  # gpu

# 是否使用该功能
split: false
gvhmr: true
detect_float: true
detect_leg: true
fix_leg: false
detect_inpenet: true
fix_inpenet: false